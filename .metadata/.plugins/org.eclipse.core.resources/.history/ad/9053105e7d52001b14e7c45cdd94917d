package Tap.Ascensores;
import Tap.Displays.Pisos;

public class Main {

	public static void main(String[] args) {
		Piso data = new DataPiso(1);
		data.addObserver(new Pisos());
		data.notifyObservers();
	}

}
